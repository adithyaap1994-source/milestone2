# Copy to .env.local for development. On Vercel, set this in Project → Settings → Environment Variables.
# Use your Render Web Service URL (https only, no trailing slash).
NEXT_PUBLIC_API_BASE_URL=https://your-service.onrender.com
